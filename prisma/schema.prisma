generator client {
  provider   = "prisma-client"
  output     = "../generated/prisma"
  engineType = "client"
}

datasource db {
  provider   = "postgres"
}

model User {
  id      String  @id @unique
  model   String?
}

model Channel {
  id      String  @id @unique
  enabled Boolean
}
